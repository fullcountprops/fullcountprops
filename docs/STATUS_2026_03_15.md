# FullCountProps — Status Report: March 15, 2026
## 11 Days to Opening Day (March 26)

### Completed Today (Sunday March 15)

**Dev Environment (built from zero)**
- Homebrew, Python 3.12, Node.js, Claude Code, gh CLI installed on local Mac
- Repo cloned, venv created, all Python deps installed (including LightGBM + libomp)
- GitHub auth configured via gh CLI (HTTPS)

**ML Pipeline (end-to-end validated)**
- 2024 Statcast data downloaded via pybaseball (710,631 pitches)
- New script: pipeline/transform_statcast_to_pa_features.py (raw pitch → PA features)
- pipeline/build_training_dataset.py fixed with single-year 80/20 fallback
- LightGBM model trained: 145,949 train / 36,487 test, 41 features, 8 outcome classes
- Model artifacts saved: models/artifacts/matchup_model.lgb

**Stripe & Payments**
- Prices aligned: $9 Double-A / $19 Triple-A / $39 The Show
- All 3 price IDs hardcoded in tiers.ts STRIPE_PRICE_TO_TIER (including $9 Double-A: price_1TBIRyCHMWdtVF7LxSqpG5r7)
- Vercel env vars updated with new price IDs
- 2 critical bugs fixed from Stripe audit: founding member env var mismatch + missing Double-A price mapping
- Old /api/v1/checkout endpoint deleted
- Stripe webhooks verified (4 events, 0% error rate)

**Routing & Navigation**
- /subscribe → /pricing redirect (SubscribeClient.tsx deleted)
- /pitchers → /pitchers/preview redirect
- All nav, footer, sitemap, 404 page links updated to /pricing
- /subscribe removed from middleware publicPaths

**Branding & Copy**
- All "BaselineMLB" / "Baseline MLB" → "FullCountProps"
- All "Pro plan" / "upgrade to Pro" → "Double-A" (MiLB naming)
- All "$7.99" → "$9" across 12+ files
- FAQ rewritten for 4-tier MiLB structure
- Compare page pricing updated
- JSON-LD structured data prices updated
- support@baselinemlb.com → support@fullcountprops.com

**Simulation Count**
- Standardized to 5,000 across 35 files
- Runtime: Makefile NUM_SIMS, simulator defaults, engine defaults, config.py
- Frontend: all user-facing strings, blog posts, methodology, OG image
- Docs: CLAUDE.md, SIMULATION_METHODOLOGY.md, ARCHITECTURE.md, README.md
- BallparkPal's 3,000 preserved as their number (not ours)

**Launch Resilience**
- Data freshness checks + StaleDataBanner on edges, most-likely, projections pages
- Feature flag: SHOW_BACKTEST_ONLY for emergency backtest mode
- Pipeline health check script + wired into GitHub Actions (both morning + afternoon jobs)
- Makefile PYTHON default fixed: python3.11 → python3

**SEO & Content**
- 3 blog posts: strikeout props guide, park factors guide, Monte Carlo explainer
- Blog index with color-coded tags, read times, CTA
- All blog routes in sitemap.ts

**Email**
- Resend API key in Vercel
- Resend domain verified (fullcountprops.com — DKIM, SPF, DMARC all green)
- Opening Day email endpoint: /api/send-opening-day-email (requires CRON_SECRET)
- Trigger script: scripts/send_opening_day_email.py

**Monitoring**
- Sentry scaffolding: client, server, edge configs + global-error.tsx + next.config.js wrapped
- Needs: create Sentry project + add DSN to Vercel

**Security**
- Supabase service_role key rotated (new sb_secret_ format)
- Updated in Vercel + GitHub Actions secrets

---

### Remaining Before Opening Day (March 26)

**P0 — Must Do:**
- [ ] Create Sentry project at sentry.io, add NEXT_PUBLIC_SENTRY_DSN + SENTRY_DSN to Vercel
- [ ] Set CRON_SECRET in Vercel env vars for email endpoint
- [ ] Test Stripe checkout end-to-end (test mode — create test sub for each tier)
- [ ] Verify GitHub Actions workflows run clean on next scheduled trigger
- [ ] Update local terminal SUPABASE_SERVICE_KEY with new rotated key

**P1 — Should Do:**
- [ ] Run full multi-year pipeline (2020-2025) for stronger model
- [ ] Load test site before Opening Day traffic
- [ ] Stage Opening Day blog post + tweet
- [ ] War room rehearsal (walk through war-room.md timeline)

**P2 — Nice to Have:**
- [ ] Annual pricing plans
- [ ] Twitter API integration for automated posting
- [ ] Daily email newsletter wiring into pipeline
