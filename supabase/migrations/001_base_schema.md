# Migration 001 — Base Schema

The base schema is defined in `supabase/schema.sql`.
This file exists as a documentation stub to explain the gap in migration numbering.

The master schema was created before the migration system was adopted.
All tables from `schema.sql` are considered migration 001.
