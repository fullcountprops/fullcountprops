# lgbm_matchup_model.txt — Model Artifact (Split into Chunks)
#
# The actual LightGBM model (1.2 MB) has been split into 20 chunks
# stored in models/artifacts/chunks/ due to repository constraints.
#
# To reconstruct the model, run from the repository root:
#
#     python models/artifacts/assemble_lgbm_model.py
#
# This creates lgbm_matchup_model.txt (~1.2 MB) in models/artifacts/.
# The assembled file is a standard LightGBM text-format model:
#
#     import lightgbm as lgb
#     booster = lgb.Booster(model_file="models/artifacts/lgbm_matchup_model.txt")
#
# Model metadata: 8-class multiclass, 416 trees, 40 features
# Trained on 248K Statcast PAs (2020-2021)
# Test log-loss: 1.4441 vs 1.4813 league avg baseline (2.5% improvement)
# Test accuracy: 46.4% vs 45.9% baseline
